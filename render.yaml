services:
  - type: web
    name: fsb-reconciliation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: FSB_SECRET
        generateValue: true   # Render auto-generates a secure random value
      - key: PYTHON_VERSION
        value: 3.11.0
